#include <iostream>
using namespace std;

int main ( ){
    // variable declaration 
    int num1, num2;
    
    // prompt user for inputs 
    cout << "enter a whole number: ";
    cin >> num1;
    cout << "enter another whole number: ";
    cin >> num2;

    // output sum to the screen
    cout << "the sum of the two numbers: " << num1 + num2;
     
    return (0);
}