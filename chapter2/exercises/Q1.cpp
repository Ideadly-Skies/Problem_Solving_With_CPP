#include <iostream>
#include <string>
using namespace std;

/* program invocation */
int main ( ) {
    
    // variable declaration
    double feet = 0, inches = 0;
    // double feet(0), inches(0);
    
    // output feet and inches
    cout << "feet: " + to_string(feet) + " feet" << endl;
    cout << "inches: " + to_string(inches) + " inches" << endl;

    // return statement
    return (0);
}